<div id="<%= dom_id external_transfer %>">
  <% if @external_transfer.accepted?%>
    <p class="text-lg text-success text-bold">Dear   <%= external_transfer&.first_name %>
      <%= external_transfer&.last_name %>, Congradulations your application is accepted 🎉</p>
    <p class="text-md text-primary text-bold"> If you want to add course exemption, please click Add course exemption button below! </p>
    <%if @external_transfer.exemptions.any?%>
       <p class="badge badge-info p-3 text-md"><%=@external_transfer.exemptions.size%> courses added for exemption, for farther information please contact the department head</p>
    <%else%>
      <p class="badge badge-danger">No Exemption yet</p>
    <%end%>
  <%elsif  @external_transfer.rejected?%>
    <p class="text-lg text-danger text-bold">Dear   <%= external_transfer&.first_name %>
      <%= external_transfer&.last_name %>, We regreat your application is not accepted!</p>
    <p class="text-md text-primary text-bold"> <%external_transfer&.message=%></p>
  <%elsif @external_transfer.pending?%>
    <p class="text-lg text-warning text-bold">Dear   <%= external_transfer&.first_name %>
      <%= external_transfer&.last_name %>, Your application is still pending</p>
    <p class="text-md text-primary text-bold"> <%external_transfer&.message=%></p>
  <%end%>
  <p class="text-md text-info">
    If you wan to check your application later, please use the your previous ID
    <strong>
      <%= external_transfer.previous_student_id %>
    </strong>
  </p>
  <p>
    <strong>Application Sent:<%= external_transfer&.created_at.strftime('%v,%T') %></strong>
  </p>

</div>
